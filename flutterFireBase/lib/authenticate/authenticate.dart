import 'package:flutter/material.dart';
import 'package:flutterFireBase/authenticate/sign_in.dart';

class Authenticate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: SignIn(),
    );
  }
}
